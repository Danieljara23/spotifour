<div class="container artist-container">
  <div class="row">

      <% @artists.each do |artist| %>
        <div class="col-sm-6 col-md-3">
          <div class="thumbnail">
            <img src="<%= artist.image_url %>" >
            <div class="caption">
              <h3 class="text-center"><%= artist.name%></h3>
              <button type="button" class="btn btn-primary btn-block" role="button" name="button"><strong>Ver más</strong></button>
            </div>
          </div>
        </div>
      <% end %>

  </div>
</div>
